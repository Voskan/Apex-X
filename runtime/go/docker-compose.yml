services:
  apexx-runtime:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    command:
      - "-addr"
      - ":8080"
      - "-adapter"
      - "onnxruntime"
      - "-default-budget-profile"
      - "balanced"

